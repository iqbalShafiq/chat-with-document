diff --git a/dist/chunk-FABSIZZ7.js b/dist/chunk-FABSIZZ7.js
index 7b8b645..9cc58d1 100644
--- a/dist/chunk-FABSIZZ7.js
+++ b/dist/chunk-FABSIZZ7.js
@@ -89,7 +89,7 @@ var ComposerRoot = forwardRef(function ComposerRoot2({
   const entitiesControlled = entitiesProp !== void 0;
   const quoteControlled = quoteProp !== void 0;
   const hasMessageContent = input.trim().length > 0 || attachments.length > 0 || entities.length > 0 || quote !== void 0;
-  const canSubmit = hasMessageContent && chat.status !== "streaming";
+  const canSubmit = hasMessageContent;
   const canStop = chat.status === "streaming";
   const setInput = useCallback(
     (nextInput) => {
@@ -160,7 +160,7 @@ var ComposerRoot = forwardRef(function ComposerRoot2({
   }, [clearQuote, setAttachments, setEntities, setInput]);
   const submit = useCallback(async () => {
     const prompt = input;
-    if (prompt.trim().length === 0 && attachments.length === 0 && entities.length === 0 && quote === void 0 || chat.status === "streaming") {
+    if (prompt.trim().length === 0 && attachments.length === 0 && entities.length === 0 && quote === void 0) {
       return;
     }
     if (submitMessage !== void 0) {
@@ -356,7 +356,7 @@ var ComposerInput = forwardRef(function ComposerInput2({
     {
       extensions,
       content: plainTextToComposerContent(composer.input),
-      editable: composer.status !== "streaming",
+      editable: true,
       editorProps: {
         attributes: {
           "aria-label": props["aria-label"] ?? "Message",
@@ -383,7 +383,7 @@ var ComposerInput = forwardRef(function ComposerInput2({
     [extensions]
   );
   const ariaDisabled = props["aria-disabled"] === true || props["aria-disabled"] === "true";
-  const composerDisabled = disabled === true || ariaDisabled || composer.status === "streaming";
+  const composerDisabled = disabled === true || ariaDisabled;
   const inputStyle = useMemo(() => {
     if (!autoResize) {
       return style;
