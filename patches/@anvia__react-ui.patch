diff --git a/dist/chunk-HWJIMLHC.js b/dist/chunk-HWJIMLHC.js
index aa2e309b451e5441ebe7f45d153a9d722f3dc368..ce5d07577123a6e50fae46507651609253b18560 100644
--- a/dist/chunk-HWJIMLHC.js
+++ b/dist/chunk-HWJIMLHC.js
@@ -91,7 +91,7 @@ var ComposerRoot = forwardRef(function ComposerRoot2({
   const quoteControlled = quoteProp !== void 0;
   const hasMessageContent = input.trim().length > 0 || attachments.length > 0 || entities.length > 0 || quote !== void 0;
   const active = chat.status === "submitted" || chat.status === "streaming";
-  const canSubmit = hasMessageContent && !active;
+  const canSubmit = hasMessageContent;
   const canStop = active;
   const setInput = useCallback(
     (nextInput) => {
@@ -162,7 +162,7 @@ var ComposerRoot = forwardRef(function ComposerRoot2({
   }, [clearQuote, setAttachments, setEntities, setInput]);
   const submit = useCallback(async () => {
     const prompt = input;
-    if (prompt.trim().length === 0 && attachments.length === 0 && entities.length === 0 && quote === void 0 || chat.status === "submitted" || chat.status === "streaming") {
+    if (prompt.trim().length === 0 && attachments.length === 0 && entities.length === 0 && quote === void 0) {
       return;
     }
     assertComposerEntitiesData(entities);
@@ -355,7 +355,7 @@ var ComposerInput = forwardRef(function ComposerInput2({
     {
       extensions,
       content: plainTextToComposerContent(composer.input),
-      editable: composer.status !== "submitted" && composer.status !== "streaming",
+      editable: true,
       editorProps: {
         attributes: {
           "aria-label": props["aria-label"] ?? "Message",
@@ -381,7 +381,7 @@ var ComposerInput = forwardRef(function ComposerInput2({
     [extensions]
   );
   const ariaDisabled = props["aria-disabled"] === true || props["aria-disabled"] === "true";
-  const composerDisabled = disabled === true || ariaDisabled || composer.status === "submitted" || composer.status === "streaming";
+  const composerDisabled = disabled === true || ariaDisabled;
   const inputStyle = useMemo(() => {
     if (!autoResize) {
       return style;
